/*
    Mongo CMDs

    in order to use mongoDB install it with npm 
        npm init

        then:
        
        npm i mongodb

    start interactive mongo shell:
        mongo

    see all databases:
        show databases

    use a certain db:
        use db

    create a certain db:
        use db (will use it if exists or create it if not)
    *db will show up with 'show databases' only once a record is inserted in it

    see all collections in a db:
        show collections

    see all elements in a collection:
        db.collection_name.find() 

    create a new collection:
        db.cars.insert({name: "Audi", price: 52642})
    * this creates a collection called 'cars' and inserts a record inside of it

    * a collection is like a table



    total time spent on playground app: 58 min;

    http://mongodb.github.io/node-mongodb-native/2.2/quick-start/quick-start/






    
*/